package main

func main() {
	// one way - decare, then assign (two steps)
	var firstNumber int
	firstNumber = 2

	// another way - declare and assign (one step)
	var secondNumber = 5

	// one step varaible: assign and declare
	subtraction := 7
}